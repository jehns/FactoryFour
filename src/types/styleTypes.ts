export type Flexbox = {
  gridSize: number,
  gutterWidth: number,
  outerMargin: number,
  mediaQuery: string,
  container: {
    xs: number,
    sm: number,
    md: number,
    lg: number
  },
  breakpoints: {
    xs: number,
    sm: number,
    md: number,
    lg: number
  }
}
